# Study results

Place scored JSON records here (one file per submission), e.g.:

```
results/
  agent_gpt4/
    loop_sum_reallang_v1.score.json
    loop_sum_c_v1.score.json
```

Generated by `score_submission.py -o ...`. These files are gitignored by default.

**No completed study data ships with the repository yet.**
