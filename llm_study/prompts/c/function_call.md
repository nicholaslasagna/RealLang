# Task: function_call (C)

Write **C11** code with helper `inc(int x)` called inside a hot loop `0..49999`, accumulating into `total`, then printing `total`.

**Expected stdout line:** `1250025000`

Output only C source.
