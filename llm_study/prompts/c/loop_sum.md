# Task: loop_sum (C)

Write a complete **C11** program that sums integers `0..49999` in a `while` loop and prints the result with `printf("%d\\n", ...)`.

Use `volatile` for loop state if needed so `-O3` cannot constant-fold the workload.

**Expected stdout line:** `1249975000`

Output only C source.
