# Task: branch_count (C)

Write **C11** code that counts even integers from `0` to `9_999_999` using a loop and `if`, then prints the count.

**Expected stdout line:** `5000000`

Output only C source.
