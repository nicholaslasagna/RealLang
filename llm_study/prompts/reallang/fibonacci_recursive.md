# Task: fibonacci_recursive (RealLang)

Write a complete **RealLang** program that:

1. Defines `fn fib(n: i32) -> i32` recursively with `if condition(...) { } else { }`.
2. In `fn main() -> i32`, prints `fib(35)` using `print_i32` and returns `0`.

**Expected stdout line:** `9227465`

Output only the RealLang source file contents.
