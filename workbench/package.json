{
  "name": "realforge-workbench-prototype",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-safe static RealForge Workbench UI prototype",
  "scripts": {
    "check": "node --check js/mock-data.js && node --check js/components.js && node --check js/app.js && node --check scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "dev": "python3 -m http.server 4173 --directory ."
  }
}
