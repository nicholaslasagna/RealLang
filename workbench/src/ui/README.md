# UI components (planned — Workbench 0.5+)

Shared presentational React components: layout shell, badges, buttons, cards, icons.

**Status:** Placeholder. The live UI is still `workbench/js/components.js` (static prototype).

Do not add new screens here until the Vite scaffold lands (see `workbench/docs/react-migration-plan.md`).
