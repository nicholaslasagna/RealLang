# App state (planned — Workbench 0.5+)

Typed store and hooks for screen routing, command palette, report import, settings,
and staff preview.

**Status:** Placeholder. State currently lives in `workbench/js/app.js` as a plain object.

Migration must preserve defaults: `staffPreview: false`, no backend mutation.
