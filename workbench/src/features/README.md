# Feature modules (planned — Workbench 0.5+)

Screen-level React modules: `reports/`, `palette/`, `settings/`, `updates/`,
`studio/`, etc.

**Status:** Placeholder. Features are rendered from `workbench/js/components.js`.

Port order: Reports → Palette → Settings → remaining screens (see migration plan).
